services:
  - type: web
    name: accpocket-api
    runtime: node
    buildCommand: pnpm install --frozen-lockfile && pnpm db:generate && pnpm --filter @accpocket/api build
    startCommand: pnpm --filter @accpocket/api start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: JWT_ACCESS_SECRET
        generateValue: true
      - key: JWT_REFRESH_SECRET
        generateValue: true
      - key: JWT_UNLOCK_SECRET
        generateValue: true
      - key: WEB_ORIGIN
        sync: false
      - key: REPORT_DETAIL_ROW_LIMIT
        value: 5000
      - key: PDF_EXPORT_ROW_LIMIT
        value: 10000
      - key: EXCEL_EXPORT_ROW_LIMIT
        value: 50000
